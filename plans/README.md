# Implementation Plans

Generated by the improve skill on 2026-06-12. Execute in the order below.
Each executor must read the complete plan, honor its STOP conditions, and
update the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Add behavior-preserving verification commands and utility tests | P1 | S | - | DONE |
| 002 | Finish Bun migration cleanup without changing runtime behavior | P2 | S | 001 | DONE |

Status values: TODO, IN PROGRESS, DONE, BLOCKED, or REJECTED.

## Dependency notes

- Plan 002 depends on plan 001 so its final `bun run check` gate includes the
  new deterministic unit tests.

## Vetted findings

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---|---|---|---|---|---|
| 1 | Add tests and an aggregate verification command | Tests, DX | Protects high-churn task, canvas, and time utilities from regressions | S | Low | `package.json:10-15`; no test files; `src/utils/taskList.ts`; `src/utils/canvas.ts`; `src/utils/formatHistoryWhen.ts` |
| 2 | Remove stale Node and Tailwind v3 metadata | Migration, docs | Prevents incorrect onboarding and dead config from accumulating | S | Low | `.nvmrc:1`; `README.md:7,18`; `eslint.config.js:15-16` |

## Findings considered and rejected

- Split `CanvasTasksContextProvider.tsx`: not worth doing now because the file
  is cohesive state orchestration, and extracting it would add indirection
  without a measurable behavior or maintenance win.
- Add a browser-test framework: deferred because the repo has no test baseline;
  pure utility tests provide higher leverage without adding dependencies.
- Upgrade React, Vite, ESLint, and TypeScript together: rejected for this pass
  because it expands migration risk and is unrelated to the requested
  behavior-preserving cleanup.
- Security remediation: no high-confidence finding. `bun audit --audit-level high`
  reported no vulnerabilities.
