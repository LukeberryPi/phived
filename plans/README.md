# Implementation Plans

Generated by the improve skill. Execute in the order below unless dependencies
say otherwise. Each executor must read the complete plan, honor its STOP
conditions, and update the status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Add behavior-preserving verification commands and utility tests | P1 | S | - | DONE |
| 002 | Finish Bun migration cleanup without changing runtime behavior | P2 | S | 001 | DONE |
| 003 | Add CI and executable site contract tests | P1 | S | - | DONE |
| 004 | Harden security headers, dependency audit, and local server binding | P1 | M | 003 | DONE |
| 005 | Harden local persistence against corrupt data and quota failures | P2 | M | 003 | DONE |
| 006 | Align cross-surface theme, docs, agent guidance, and TypeScript | P2 | M | 003 | DONE |

Status values: TODO, IN PROGRESS, DONE, BLOCKED, or REJECTED.

## Dependency notes

- Plan 002 depends on plan 001 so its final `bun run check` gate includes the
  deterministic unit tests.
- Plans 004, 005, and 006 depend on plan 003 so routing/CI checks exist before
  broader security, persistence, and cross-surface changes.

## Vetted findings

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---|---|---|---|---|---|
| 1 | Add tests and an aggregate verification command | Tests, DX | Protects high-churn task, canvas, and time utilities from regressions | S | Low | Historical monolith paths; completed by 001 |
| 2 | Remove stale Node and Tailwind v3 metadata | Migration, docs | Prevents incorrect onboarding and dead config from accumulating | S | Low | Historical monolith paths; completed by 002 |
| 3 | Add CI and executable site-contract tests | Tests, DX | Enforces `/`, `/app`, and `/sw.js` deployment invariants | S | Low | `scripts/site-contract.mjs`; `scripts/build-site.mjs`; no `.github` workflow |
| 4 | Harden security headers and local server binding | Security | Reduces browser-origin and local-network exposure; remediates high audit finding if possible | M | Medium | `vercel.json`; `apps/app/index.html`; `scripts/dev-site.mjs`; `bun audit --audit-level high` |
| 5 | Harden local persistence | Correctness | Prevents corrupt storage, quota failures, and unbounded history from silently breaking or losing task data | M | Medium | `apps/app/src/hooks/useLocalStorage.ts`; `apps/app/src/hooks/useCanvasViewport.ts`; `CanvasTasksContextProvider.tsx` |
| 6 | Align cross-surface consistency | Docs, migration | Keeps web/app/macOS copy, theme behavior, agent guidance, and TS versions from drifting after the split | M | Medium | `apps/web/src/pages/index.astro`; `.cursor/agents/phived-ui.md`; workspace package manifests |

## Findings considered and rejected

- Split `CanvasTasksContextProvider.tsx`: still not first-order until persistence
  characterization tests exist; revisit after plan 005.
- React 19 migration: no repo evidence that it unblocks product work today.
- Browser E2E framework: useful later, but site-contract unit/smoke tests have
  better leverage first.
